{
  "name": "secureme-selenium-tests",
  "version": "1.0.0",
  "description": "Selenium E2E Automated Testing Suite for SecureMe Web Frontend",
  "main": "tests/login-tests.js",
  "scripts": {
    "test": "mocha tests/**/*.js --timeout 60000 --reporter spec",
    "test:live": "mocha tests/**/*.js --timeout 60000 --reporter spec",
    "generate-report": "python generate_selenium_report.py"
  },

  "dependencies": {
    "selenium-webdriver": "^4.20.0",
    "chromedriver": "^124.0.0",
    "mocha": "^10.4.0",
    "chai": "^4.3.10"
  }
}
